import { api, state, shell, bindShell } from '../main.js';
const esc = v => { const e=document.createElement('span'); e.textContent=String(v??''); return e.innerHTML; };
export async function paymentsScreen(app) { app.innerHTML=shell(`<div class="panel"><form id="payment" class="add-form"><h2>Nova cobrança</h2>${state.session.user.role==='admin'?'<input name="organization" placeholder="ORG_0001" required>':''}<input name="name" placeholder="Cliente" required><input name="cpfCnpj" placeholder="CPF/CNPJ" required><input name="email" type="email" placeholder="E-mail"><input name="value" type="number" min="5" step=".01" placeholder="Valor" required><select name="billingType"><option value="PIX">Pix</option><option value="BOLETO">Boleto</option><option value="CREDIT_CARD">Cartão</option></select><input name="dueDate" type="date" required><input name="expiresAt" type="datetime-local" required><button>Criar cobrança</button></form><p id="pay-error" class="error"></p><div id="payments">Carregando...</div></div>`, 'Sistema de Pagamento'); bindShell(); const form=app.querySelector('#payment'); const future=new Date(Date.now()+7*86400000); form.dueDate.value=future.toISOString().slice(0,10); form.expiresAt.value=future.toISOString().slice(0,16); form.onsubmit=async e=>{e.preventDefault(); try { const body=Object.fromEntries(new FormData(form)); body.value=Number(body.value); body.expiresAt=new Date(body.expiresAt).toISOString(); await api('/api/payments',{method:'POST',body:JSON.stringify(body)}); await paymentsScreen(app); } catch(err){app.querySelector('#pay-error').textContent=err.message;} }; try { const data=await api('/api/payments'); const rows=data.payments?.data||[]; app.querySelector('#payments').innerHTML=`<h2>Cobranças recentes</h2>${rows.map(p=>`<div class="user"><strong>${esc(p.externalReference)}</strong><span>${esc(p.dueDate)}</span><span>R$ ${Number(p.value||0).toFixed(2).replace('.',',')}</span><span class="badge">${esc(p.status)}</span></div>`).join('')||'<p>Nenhuma cobrança encontrada.</p>'}`; } catch(err){ app.querySelector('#payments').innerHTML=`<p class="error">${esc(err.message)}</p>`; } }
